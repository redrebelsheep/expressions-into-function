// Clean up the code below with a single function called `personalIntroduction`
// The new code should have the exact same functionality and output.
// new Version form the same code marian kowall 
//Organizing Code with Functions

const createPerson = (name, height, favoriteColor) =>  console.log("My name is " + name + " and my height is " + height + " and my favorite color is " + favoriteColor);


createPerson("Bob", "72", "red");
createPerson("Tamera", "63", "blue");
createPerson("Christopher", "69", "green");
createPerson("Samantha", "65", "orange");